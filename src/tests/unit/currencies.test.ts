describe("currencies", () => {
    it('should_create_curencies', ()=>{
        const one: number = 1;
        const two: number = 1;
        expect(one).toEqual(two);
    })

    it('should_edit_curencies', ()=>{
        const one: number = 1;
        const two: number = 1;
        expect(one).toEqual(two);
    })

    it('should_delete_curencies', ()=>{
        const one: number = 1;
        const two: number = 1;
        expect(one).toEqual(two);
    })

    it('should_fetch_curencies_for_an_orgaisation', ()=>{
        const one: number = 1;
        const two: number = 1;
        expect(one).toEqual(two);
    })

    it('should_search_curencies_for_an_orgaisation', ()=>{
        const one: number = 1;
        const two: number = 1;
        expect(one).toEqual(two);
    })
});
