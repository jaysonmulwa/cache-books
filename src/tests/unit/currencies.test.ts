describe("currencies", () => {
    it('should_create_currencies', ()=>{
        const one: number = 1;
        const two: number = 1;
        expect(one).toEqual(two);
    })

    it('should_edit_currencies', ()=>{
        const one: number = 1;
        const two: number = 1;
        expect(one).toEqual(two);
    })

    it('should_delete_currencies', ()=>{
        const one: number = 1;
        const two: number = 1;
        expect(one).toEqual(two);
    })

    it('should_fetch_currencies_for_an_orgaisation', ()=>{
        const one: number = 1;
        const two: number = 1;
        expect(one).toEqual(two);
    })

    it('should_search_currencies_for_an_orgaisation', ()=>{
        const one: number = 1;
        const two: number = 1;
        expect(one).toEqual(two);
    })
});
