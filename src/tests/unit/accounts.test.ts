describe("chart_of_accounts", () => {
    it('should_create_accounts', ()=>{
        const one: number = 1;
        const two: number = 1;
        expect(one).toEqual(two);
    })

    it('should_edit_accounts', ()=>{
        const one: number = 1;
        const two: number = 1;
        expect(one).toEqual(two);
    })

    it('should_delete_accounts', ()=>{
        const one: number = 1;
        const two: number = 1;
        expect(one).toEqual(two);
    })

    it('should_fetch_accounts_for_an_orgaisation', ()=>{
        const one: number = 1;
        const two: number = 1;
        expect(one).toEqual(two);
    })

    it('should_search_accounts_for_an_orgaisation', ()=>{
        const one: number = 1;
        const two: number = 1;
        expect(one).toEqual(two);
    })
});
