describe("entry_types", () => {
    it('should_create_entry_types', ()=>{
        const one: number = 1;
        const two: number = 1;
        expect(one).toEqual(two);
    })

    it('should_edit_entry_types', ()=>{
        const one: number = 1;
        const two: number = 1;
        expect(one).toEqual(two);
    })

    it('should_delete_entry_types', ()=>{
        const one: number = 1;
        const two: number = 1;
        expect(one).toEqual(two);
    })
});
