"use strict";
describe("organizations", () => {
    it('should_create_organizations', () => {
        const one = 1;
        const two = 1;
        expect(one).toEqual(two);
    });
    it('should_edit_organizations', () => {
        const one = 1;
        const two = 1;
        expect(one).toEqual(two);
    });
    it('should_delete_organizations', () => {
        const one = 1;
        const two = 1;
        expect(one).toEqual(two);
    });
    it('should_search_organizations_for_an_orgaisation', () => {
        const one = 1;
        const two = 1;
        expect(one).toEqual(two);
    });
});
