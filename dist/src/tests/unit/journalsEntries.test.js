"use strict";
describe("journal_acc_entries", () => {
    it('should_create_journal_acc_entries', () => {
        const one = 1;
        const two = 1;
        expect(one).toEqual(two);
    });
    it('should_edit_journal_acc_entries', () => {
        const one = 1;
        const two = 1;
        expect(one).toEqual(two);
    });
    it('should_delete_journal_acc_entries', () => {
        const one = 1;
        const two = 1;
        expect(one).toEqual(two);
    });
    it('should_fetch_journal_acc_entries_for_an_orgaisation', () => {
        const one = 1;
        const two = 1;
        expect(one).toEqual(two);
    });
});
